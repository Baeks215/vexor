

fn make_circle(i) = Circle(r) >> fill(color) >> move(x, y)
where {
  f = i / 60
  r = f * f * 100
  angle = 360 * f
  x = cos(angle) * f * 400
  y = sin(angle) * f * 400
  color = rgb(f, 0, 1 - f, 1)
}

export move(500, 500)(Group(
  map(make_circle)([1..60])
))
