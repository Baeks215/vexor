All tests in the "cases" directory will be tested.

The `// OUTPUT: x` comment indicates an expected output.
The `// OUTPUT: x, y, z` comment indicates expected outputs from multiple exports.

Since vexor only exports graphic components, we take the radius from exported circles as outputs of the program.
